<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<title>FREQUENCIES // LEVEL 04</title>
<style>
  body { background:#050510; color:#8b9248; font-family:'Courier New', monospace;
         min-height:100vh; display:flex; align-items:center; justify-content:center; }
  .container { max-width:600px; padding:40px; border:1px solid #8b9248;
               background:rgba(10,10,25,0.9); box-shadow:0 0 40px rgba(139,146,72,0.3); }
  h1 { font-size:22px; letter-spacing:4px; text-align:center; margin-bottom:10px; }
  .status { font-size:12px; color:#444; text-align:center; margin-bottom:25px; letter-spacing:2px; }
  .status .online { color:#00ff88; }
  pre { background:#0a0a1a; border-left:2px solid #8b9248; padding:20px;
        color:#8b9248; font-size:14px; line-height:1.6; }
  .hint { font-size:12px; color:#666; margin-top:20px; line-height:1.7; }
  .hint b { color:#8b9248; }
  .input-group { display:flex; gap:10px; margin-top:20px; }
  input { flex:1; padding:12px 16px; background:#0a0a1a; border:1px solid #8b9248;
          color:#8b9248; font-family:'Courier New', monospace; outline:none; }
  button { padding:12px 24px; background:transparent; border:1px solid #8b9248;
           color:#8b9248; font-family:'Courier New', monospace; cursor:pointer; }
  button:hover { background:#8b9248; color:#050510; }
  .log { font-size:12px; margin-top:15px; min-height:20px; color:#555; }
  .log .error { color:#ff3366; }
  .log .success { color:#00ff88; }
</style>
</head>
<body>
<div class="container">
  <h1>FREQUENCY LOG</h1>
  <div class="status">
    NEURAL LINK <span class="online">● ONLINE</span><br>
    LEVEL 04 / 13
  </div>

  <pre>
Harmonics of the heartbeat:
440
880
1320
1760
2200
2640
3080
3520
  </pre>

  <div class="hint">
    // Это гармоники одной ноты.<br>
    // <b>Сложи их</b> — получишь ключ к следующей точке на карте.
  </div>

  <div class="input-group">
    <input type="text" id="keyInput" placeholder="СУММА" autocomplete="off">
    <button onclick="checkKey()">LINK</button>
  </div>

  <div class="log" id="log"></div>
</div>

<script>
  const CORRECT = "15840";
  const NEXT = "/map.html";
  function checkKey() {
    const v = document.getElementById("keyInput").value.trim();
    const log = document.getElementById("log");
    if (v === CORRECT) {
      log.innerHTML = '<span class="success">>> SUM = 15840. Coordinates unlocked.</span>';
      setTimeout(() => { window.location.href = NEXT; }, 1500);
    } else {
      log.innerHTML = '<span class="error">>> WRONG SUM.</span>';
    }
  }
  document.getElementById("keyInput").addEventListener("keypress", e => {
    if (e.key === "Enter") checkKey();
  });
</script>
</body>
</html>